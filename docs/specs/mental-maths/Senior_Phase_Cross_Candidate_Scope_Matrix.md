# Senior Phase Mental Maths — Cross-Candidate Scope Matrix

> **NOT TAXONOMY AUTHORITY. NOT AN IMPLEMENTATION AUTHORIZATION.**
> This document consolidates the five scope-decision reviews performed
> to date (`mulDivFluency`, `powersRootsFluency`, `ratioSharing`,
> `addSub`, `fracDecPercent`) into one structural view, so that any
> eventual taxonomy decision is made once, consistently, across the
> whole candidate universe, rather than as five local decisions that
> could contradict each other. It assigns no taxonomy status, freezes
> nothing, and creates no Generation Policy or specification. It
> supersedes no existing document.

## Repository-record caveat — read before using this matrix

*(Updated by documentary reconciliation — see note below.)* As of
this reconciliation, **all five candidates' scope-decision reviews are
committed to `origin/main`**:

| Candidate | Scope-decision review committed to repo? |
|---|---|
| mulDivFluency | **Yes** — `mulDivFluency_Scope_Decision_Review.md`, on record |
| fracDecPercent | **Yes** — `fracDecPercent_Scope_Decision_Review.md` (`0416ac8`) + `fracDecPercent_Deeper_Evidence_Review_Checkpoint.md` (`cb0ee56`) |
| powersRootsFluency | **Yes** — `powersRootsFluency_Scope_Decision_Review__RECONSTRUCTION.md` (`2015a10`), committed after this matrix was first written |
| ratioSharing | **Yes** — `ratioSharing_Scope_Decision_Review.md` (`63a1beb`), committed after this matrix was first written |
| addSub | **Yes** — `addSub_Scope_Decision_Review.md` (`76d6f4a`), committed after this matrix was first written |

The gap this caveat originally flagged is now closed at the commit
level. This does **not** mean all five scope questions are resolved —
see "Cross-cutting patterns" §2 below for which two now have proposed
(not frozen) outcomes, and which three remain OPEN or unstatused.

## The matrix

| | mulDivFluency | powersRootsFluency | ratioSharing | addSub | fracDecPercent |
|---|---|---|---|---|---|
| **CAPS-named units/sub-constructs** | 2: multiplication-fact fluency; inverse multiplication/division | 1 named topic ("1.2 Exponents") internally covering: squares/cubes/roots; general exponent laws; scientific notation — CAPS does not sub-divide these into separately numbered topics the way 1.4/1.5 divide fractions/decimals | 1 CAPS bullet ("sharing in a given ratio") that is itself one of 4 sibling bullets in a "Solving problems" list (ratio, rate, sharing-in-ratio, increasing/decreasing-in-ratio) | 1 named property ("addition and subtraction as inverse operations") that is 1 of 7 sibling properties under "Properties of whole numbers" | 4 separately-numbered CAPS topics: 1.4 Calculations with Fractions, 1.4 Percentages, 1.4 Equivalent Forms, 1.5 Decimal Fractions (Decimal Fractions confirmed standalone only in the deeper pass) |
| **Does CAPS group or separate them?** | Separates — two distinct Section 2 sub-bullets, not one combined phrase | Effectively groups — one numbered topic, three internal mechanisms, no CAPS-given sub-division | Separates — four sibling bullets under one heading, but `ratioSharing` and its `ratioRate` counterpart are already split as two candidates in this project with no located justification | Separates — seven sibling properties, all named together in one clarification-notes list, no CAPS grouping into pairs or subsets | Separates — four distinct numbered topics, each with independent G7/G8/G9 rows and its own clarification notes |
| **Evidence tier per sub-construct** | Fact fluency: named-but-unworked. Inverse op: named-and-worked at G7 | Uniform and strong — named-and-worked at every grade (G7/G8/G9) for squares/cubes/roots, exponent laws, and scientific notation alike; G8→G9 explicit CAPS continuity statement located | Named-but-unworked (no worked example for sharing-in-a-ratio located in the evidence checkpoint) | Named-and-worked (G7/G8, one worked example located) for the inverse-operations property specifically; the other 6 sibling properties are unassessed by any evidence checkpoint in this project | Uneven: Equivalent Forms and Percentage Calculation each independently named-and-worked at G7; Calculations with Fractions named-but-unworked in the original pass, later found worked at G8 (addition/multiplication/division) in the deeper pass; Percentages found richly worked at G8 (5 distinct mechanisms) in the deeper pass, no standalone G9 heading; Decimal Fractions worked at G7/G8 (multiplication, division, and — only for decimals, not common fractions — squares/roots), unworked at G9 |
| **Existing candidate split already in place?** | No — one candidate | No — one candidate | **Yes** — `ratioSharing` and a `ratioRate`-equivalent already exist as two separate candidates | No — one candidate (the other 6 sibling properties have no candidate name at all, named or unnamed) | No — one candidate (`fracDecPercent`) covering all four now-identified CAPS topics |
| **Basis located for any existing split** | N/A | N/A | **None found** — no CAPS statement or governance document explaining why ratio/rate/sharing/increasing-decreasing were divided into exactly two candidates rather than one, three, or four | N/A | N/A |
| **Cross-candidate overlap with another candidate** | None located | None located as a *source* of overlap, but is the *target* of two separate overlap questions raised elsewhere: addSub's inverse-operations material sits in the same CAPS list as mulDivFluency's own inverse-operations bullet (same "Properties of whole numbers" list); fracDecPercent's G9 "squares/cubes/roots of common and decimal fractions" material extends the same mechanism family evidenced here | None located | **Yes** — addSub's "addition and subtraction as inverse operations" and mulDivFluency's "inverse operation between multiplication and division" are evidenced from the same CAPS clarification-notes list (items (e) and (f) respectively); no governance decision on which candidate this belongs to, or whether it is one shared candidate | **Yes** — fracDecPercent's G9 "all four operations... that involve the squares, cubes, square roots and cube roots of common/decimal fractions" is the same rational-number extension already evidenced under powersRootsFluency's G8 continuity; unresolved which candidate it belongs to |
| **Existing precedent applied** | C1 (Doubling/Halving) — bundled-but-uneven-tier precedent, resolved as OPEN in the frozen Grade 5 spec | None directly applied in the committed evidence checkpoint (no scope-decision review on record) | None found — the `ratioRate` precedent this candidate would naturally be compared against has no locatable document in this repository | None directly cited; addSub's own review instead identifies a *new* structural shape (name doesn't match any single CAPS unit) not seen in mulDivFluency or C1 | mulDivFluency's mixed-tier-bundle shape (for the internal grouping question) and addSub's shared-sentence-boundary shape (for the G9 powersRootsFluency overlap question) are both invoked as partial analogies, not full precedent matches |
| **Affirmative evidence to bundle as one candidate** | Not located | Not located as an explicit statement, but CAPS's own single-topic numbering ("1.2 Exponents") is the closest thing to affirmative bundling evidence found anywhere in this matrix | Not located | Not located | Not located — CAPS numbers these as four separate topics (1.4 ×3, 1.5 ×1), which if anything weakly supports splitting, not bundling |
| **Affirmative evidence to split** | Not located (though CAPS's two-bullet structure is suggestive) | Not located | Not located (though the four-way CAPS bullet structure is suggestive) | Not located (though the seven-way CAPS list structure is suggestive) | Not located as a decision, but CAPS's topic numbering (1.4/1.4/1.4/1.5) is the most structurally suggestive split evidence of any candidate in this matrix |
| **What remains genuinely undecidable from evidence alone** | Whether "related-but-CAPS-separated" bullets should default to one candidate or many when evidence tiers differ — no project rule resolves this generally | Whether squares/cubes/roots, exponent laws, and scientific notation should be one candidate (as CAPS's numbering suggests) or three (given they are conceptually distinct skills evidenced independently at each grade) | Whether the pre-existing ratioSharing/ratioRate split reflects a real decision this project once made and lost the record of, or was arbitrary from the start | Whether "properties of whole numbers" is itself the correct candidate-level unit (absorbing all 7 properties) or whether some subset (e.g. just the two inverse-operation properties, shared with mulDivFluency) is the right grain | Whether the CAPS numbering (1.4 ×3 + 1.5) should be taken as authoritative bundling/splitting signal at all, given this project's standing rule that named-topic structure is evidence but not automatically decisive, and given G9's percentage-heading absence complicates a clean four-way split |

## Cross-cutting patterns visible only at this consolidated level

1. **Every candidate in this matrix shows the same shape**: CAPS names
   sub-constructs separately (by bullet, by list item, or by numbered
   topic), while this project's legacy candidate names bundle some or
   all of them together, with no affirmative CAPS or governance
   statement located anywhere in this matrix that requires the bundle.
   This is not a coincidence specific to any one candidate — it is a
   pattern across all five, and may itself be worth naming as a
   standing finding before any individual decisions are made.

2. **Two distinct kinds of overlap were identified**, and were
   resolved differently, though both remain proposed rather than
   frozen:
   - **Shared-sentence overlaps** (addSub ↔ mulDivFluency): the exact
     same CAPS worked material is cited as evidence for two different
     candidates. **A proposed scope decision now exists** (`b6dba51`):
     a proposed single inverse-operations candidate combining the two
     inverse-operation constructs. This is a **proposed, not frozen**
     outcome — no ADR-023 §6 freeze act has occurred.
   - **Shared-mechanism-family overlaps** (fracDecPercent ↔
     powersRootsFluency): different CAPS material, evidencing the same
     underlying mathematical mechanism (squares/cubes/roots) applied to
     different number types (integers vs. common/decimal fractions).
     **A proposed scope decision now exists** (`5b4c964`): the
     rational-number squares/cubes/roots material is proposed to
     remain within `powersRootsFluency`, as representations of one
     candidate, rather than as a separate "extends to" relationship or
     a representation-level split. This is also a **proposed, not
     frozen** outcome.

   The distinction this matrix originally drew — a shared sentence
   tending to argue for merging, a shared mechanism-family instead
   leaving an "extends to" relationship as a live option — is
   preserved as background reasoning; in both cases here the Project
   Owner's proposed resolution was to merge rather than retain an
   "extends to" relationship. Neither resolution constitutes a
   taxonomy freeze, and this matrix does not itself adopt or ratify
   either decision record — it only reports that they exist.

3. **Only one candidate (`ratioSharing`) involves an *already-existing*
   split with no located justification**, as opposed to an *unmade*
   bundling/splitting decision. This is a different kind of open
   question — the other four candidates ask "should we split/bundle,"
   while ratioSharing asks "was the existing split ever justified,"
   and its resolution may need to look for a lost document rather than
   new CAPS evidence.

4. **fracDecPercent is now the structurally richest candidate in this
   matrix** (4 CAPS-named topics, 2 confirmed cross-candidate
   overlaps — one via shared mechanism family, contributed by its own
   G9 material) — a materially different shape than it had before the
   deeper evidence pass, when it looked like a 3-topic candidate with
   1 overlap.

## Non-inference constraints in force

- This matrix draws only on already-committed evidence and scope-
  decision documents (see the repository-record caveat above for which
  candidates that includes) plus, where noted, uploaded-but-uncommitted
  content from this working session. It performs no new CAPS
  extraction of its own.
- No cell in this matrix should be read as a taxonomy decision. The
  "affirmative evidence to bundle/split" rows report what evidence
  exists, not what should be concluded from it.
- CAPS's own topic-numbering structure (e.g. "1.4" vs. "1.5") is
  recorded as observed structural evidence, not treated as
  automatically decisive — consistent with this project's standing
  posture that named-topic structure informs but does not resolve
  scope questions on its own.
- The absence of a committed scope-decision review for
  powersRootsFluency, ratioSharing, and addSub must not be read as
  those candidates being less "open" than mulDivFluency or
  fracDecPercent — their OPEN status is unchanged; only the repository
  paper trail differs.
- No taxonomy status, Generation Policy, implementation, or test
  change is authorized by this document.

## What can now be checkpointed

This cross-candidate matrix is ready to checkpoint as a consolidation
artifact. It does not itself resolve any scope question to a frozen
taxonomy outcome. The five scope reviews now have differing
substantive states: two cross-candidate relationships have proposed,
not frozen outcomes recorded in dedicated decision records;
`ratioSharing` remains **C — explicitly OPEN**; and the five remaining
whole-number-properties siblings remain unresolved/unstatused. This
matrix does not itself adopt, ratify, freeze, or authorize any of
those outcomes. The repository-record gap for the
`powersRootsFluency`, `ratioSharing`, and `addSub` scope-decision
reviews is now closed at the commit level. This documentary
reconciliation does not, however, resolve the substantive scope
questions themselves beyond the proposed outcomes explicitly recorded
in the dedicated decision records.

## Suggested next governance step (not authorized by this document)

1. ~~Commit the powersRootsFluency, ratioSharing, and addSub
   scope-decision reviews~~ — **done**; all five candidates'
   scope-decision reviews are now committed (see the repository-record
   caveat above).
2. Two of the overlaps this matrix identified now have proposed (not
   frozen) scope decisions on record (`b6dba51`, `5b4c964`).
   `ratioSharing`'s external split-vs-`ratioRate` question remains
   explicitly OPEN, and the five other sibling whole-number properties
   remain unresolved/unstatused. Any further consolidated governance
   pass, and any eventual freeze act for either proposed decision,
   remains a separate, explicit Project Owner act under ADR-023 §6 —
   not implied or performed by this documentary update.

Neither a freeze act nor a taxonomy/`AUTHORIZED_FAMILIES` update is
undertaken by this document.

---

End of cross-candidate scope matrix. The five candidate scope reviews
are now committed to the repository. Their substantive governance
states are not uniform: `ratioSharing` remains **C — explicitly
OPEN**; the `fracDecPercent` ↔ `powersRootsFluency` relationship and
the `addSub` ↔ `mulDivFluency` relationship each now have **proposed,
not frozen** scope outcomes recorded in dedicated decision records;
and the five remaining whole-number-properties siblings remain
unresolved and unstatused. No taxonomy freeze has occurred. No
taxonomy edits. No Generation Policy. No implementation or test
changes.
