# ratioSharing — Scope-Decision Review

> **NOT TAXONOMY AUTHORITY. NOT AN IMPLEMENTATION AUTHORIZATION.**
> This document extends `ratioSharing_Evidence_Review_Checkpoint.md`
> with a scope-decision review. It assigns no taxonomy status, freezes
> nothing, and creates no Generation Policy or specification. It
> supersedes no existing document.

## Repository baseline at time of this checkpoint

`7356644` on `origin/main`, plus
`powersRootsFluency_Scope_Decision_Review.md` added earlier in this
same session. This checkpoint commit adds no code, taxonomy, or
Generation Policy changes on top of that baseline.

## Evidence boundary in force for this decision

Only `ratioSharing_Evidence_Review_Checkpoint.md` is treated as
authoritative. That checkpoint established:

- G7/G8: named phrase "sharing in a given ratio where the whole is
  given," as one bullet among several under a shared "Solving
  problems" heading that also includes "comparing two or more
  quantities of the same kind (ratio)," "comparing two quantities of
  different kinds (rate)," and (G8 only) "increasing or decreasing of
  a number in a given ratio."
- No worked example located for sharing-in-a-ratio specifically.
- G9: the phrase is not repeated; G9's bullets are the broader "ratio
  and rate" and "direct and indirect proportion," with no explicit
  statement of whether these subsume or drop the G7/G8 mechanism.

## A note on what this document cannot do

`mulDivFluency_Scope_Decision_Review.md` and other prior checkpoints
in this repository refer to an existing "ratioRate scope review...
already on record in this project." **That document does not exist as
a file in this repository** — it is referenced only in prose by other
checkpoints, with no corresponding
`ratioRate_Scope_Decision_Review.md` or equivalent findable via
`git log` or file search. This review therefore cannot compare against
`ratioRate`'s actual reasoning or evidence — only against what other
documents say it concluded (a "heterogeneous constructs, scope open"
finding, per `Candidate_Universe_Status_Consolidation.md`). This
absence is noted as a gap in the project's own paper trail, not
assumed to be this document's job to fill.

## The core scope question for ratioSharing

Unlike `mulDivFluency` and `powersRootsFluency`, `ratioSharing`'s scope
question is not "should sub-constructs within this candidate be
bundled or split" — its own single bullet ("sharing in a given ratio
where the whole is given") is one CAPS phrase, not several. Its real
scope question is **external**: how it relates to the separately-named
`ratioRate` candidate, given both:

- sit under the same CAPS "Solving problems" bullet group at G7/G8
  (ratio, rate, sharing-in-a-ratio, and at G8 increasing/decreasing-
  in-a-ratio, are four sibling bullets in the same list), and
- are already split into two separate candidates in this project's
  `AUTHORIZED_FAMILIES` naming (`ratioSharing` and — per the legacy
  generator naming pattern referenced elsewhere in this repository —
  a `ratioRate`-equivalent), without any on-record CAPS or governance
  justification located for that split, given the ratioRate review
  document itself cannot be located.

## Structural comparison

| | mulDivFluency | ratioSharing (vs. ratioRate) |
|---|---|---|
| Are the related items named as sibling bullets under one heading? | Yes (two bullets, one heading) | Yes (four sibling bullets, one heading: ratio, rate, sharing-in-ratio, increasing/decreasing-in-ratio) |
| Evidence tier of each | Mixed | ratioSharing: named-but-unworked. ratioRate's tier is unknown to this document (source file absent) |
| Existing candidate split already in place? | No (mulDivFluency is one candidate) | Yes — ratioSharing and ratioRate already exist as two separate candidates |
| Basis located for the existing split | N/A | **None found** — no CAPS statement or governance document explaining why these four sibling bullets were divided into exactly two candidates (ratio+rate together, sharing+increasing/decreasing together, or some other split) rather than one, three, or four |

## Which of A / B / C is supported

**C — the scope question remains explicitly OPEN**, for a different
reason than `mulDivFluency`'s or `powersRootsFluency`'s: here the
question is not whether to bundle or split evidence this review
gathered, but whether an **already-existing** split (ratioSharing vs.
ratioRate as separate candidates) has any evidentiary basis at all.
This review did not find one. Recording C is the honest position:
neither ratifying the existing split (no evidence supports it as
correct) nor undoing it (no evidence supports merging either, and
`ratioRate`'s own review, which might contain such evidence, could not
be located to check).

## What remains OPEN, precisely

- Whether `ratioSharing` and `ratioRate` should remain separate
  candidates, be merged, or be split differently (e.g. along the four
  CAPS sibling bullets individually) — unresolved, and now flagged as
  resting on a missing source document.
- G9 continuity for sharing-in-a-ratio specifically — genuinely
  unresolved per the evidence checkpoint, not merely unretrieved.
- Whether a worked example for sharing-in-a-ratio exists elsewhere in
  the CAPS document (e.g. assessment exemplars) not yet searched.

## Non-inference constraint in force

- The existing `AUTHORIZED_FAMILIES` split between `ratioSharing` and
  a ratioRate-equivalent must not be treated as evidence that the
  split is correct, per this project's standing rule that
  implementation/naming is not specification authority.
- The absence of a locatable `ratioRate` scope-review file must not be
  read as evidence that no such review was ever done — only that it is
  not present in this repository as this document finds it.
- Neither bundling nor splitting is authorized by this document.

## What can now be checkpointed

This scope-decision review is ready to checkpoint. No taxonomy status,
Generation Policy, implementation, or test change accompanies it.

## Next candidate

Per the instructed sequence: `addSub` next.

---

End of scope-decision review. Scope question **C (explicitly OPEN)**
recorded, with the additional finding that the existing ratioSharing/
ratioRate split has no locatable evidentiary basis in this repository.
No taxonomy edits. No Generation Policy. No implementation or test
changes.